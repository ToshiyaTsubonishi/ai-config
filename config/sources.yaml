version: "1.0.0"

# MCP source repositories managed as git submodules by ai-config-sources.
# External skill source seeds now live in config/vendor_skills.yaml and are
# materialized into skills/external by ai-config-vendor-skills sync-manifest.

sources:
  # example-mcp:
  #   type: mcp
  #   url: https://github.com/example/mcp-server
  #   path: mcp/external/example-mcp
  #   branch: main
